# ⚙️ Скрипты для разработки

Эта папка содержит конфигурационные файлы для инструментов анализа кода.

## 📁 Структура

| Файл | Назначение |
|------|------------|
| `test_ci.sh` | Локальная проверка CI/CD pipeline |
| `check_code.sh` | Комплексная проверка качества кода |
| `precommit.sh` | Управление pre-commit хуками |

## 🚀 test_ci.sh
Локальная проверка всех этапов CI/CD pipeline перед отправкой кода в репозиторий.

### Что проверяет:
- **Lint Job**: Ruff linter, форматирование, MyPy
- **Test Job**: Запуск тестов с покрытием кода
- **Build Job**: Проверка сборки Docker образа

### Использование:
```bash
# Запуск полной проверки CI/CD
./scripts/test_ci.sh
```

**Рекомендация:** Запускайте этот скрипт перед каждым push, чтобы убедиться, что код пройдет GitHub Actions.

## 🔍 check_code.sh
Комплексная проверка качества кода.

### Что проверяет:
- Форматирование кода (Ruff format)
- Статический анализ (Ruff lint)
- Проверка типов (MyPy)
- Pre-commit хуки

### Использование:
```bash
# Запуск всех проверок
./check_code.sh
```

## ⚡ precommit.sh
Управление pre-commit хуками.

### Использование:
```bash
# Установка pre-commit хуков
./precommit.sh install

# Запуск всех хуков
./precommit.sh run

# Запуск хуков на всех файлах
./precommit.sh run --all-files

# Обновление хуков
./precommit.sh autoupdate

# Удаление хуков
./precommit.sh uninstall
```

## 📝 Примечания

- Все скрипты должны запускаться из папки корня проекта
- Скрипты автоматически переходят в нужную директорию
- При первом запуске может потребоваться `chmod +x *.sh`
